import type { ClientInfo } from '../types/gallery';

export const CLIENT_CONFIG: ClientInfo = {
  name: "CollabSpace",
  description: "Cette galerie vous propose l'ensemble des photos créées par Imprononçable pour vos projets. Vous pouvez les filtrer par catégorie, par type de droits, puis les prévisualiser et les télécharger."
};